[project]
name = "server"
version = "0.1.0"
description = "backend for moneta"
authors = [
    {name = "Kamil Aliyev",email = "kamilraliyev@gmail.com"}
]
readme = "README.md"
requires-python = ">3.8, <=3.9"
dependencies = [
    "fastapi (>=0.126.0,<0.127.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
