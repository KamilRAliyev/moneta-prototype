# PostgreSQL Client Authentication Configuration
# Allow connections from Docker network and localhost (dev only)
# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                  scram-sha-256
# Docker bridge networks (e.g. 172.17.x, 172.18.x)
host    all             all             172.16.0.0/12            scram-sha-256
# Docker Desktop for Mac: host appears as 192.168.65.x when connecting via localhost:5432
host    all             all             192.168.0.0/16           scram-sha-256
# Any other host (dev only)
host    all             all             0.0.0.0/0                scram-sha-256
