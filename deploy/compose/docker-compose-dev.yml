services:
  postgres:
    image: postgres:16-alpine
    container_name: illiterate_monkey_db
    env_file:
      - ../../env/dev.env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U moneta"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - monkey_network

  pg_admin:
    image: dpage/pgadmin4:latest
    container_name: illiterate_monkey_pgadmin
    env_file:
      - ../../env/dev.env
    ports:
      - "8080:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    networks:
      - monkey_network

  app:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    container_name: illiterate_monkey_app
    env_file:
      - ../../env/dev.env
    ports:
      - "8000:8000"  # Backend API
      - "5173:5173"   # Frontend Vite dev server
    volumes:
      # Mount backend directory for live code changes
      - ../../backend:/app/backend
      # Mount frontend directory for live code changes
      - ../../frontend:/app/frontend
      # Mount env directory for environment variables
      - ../../env:/app/env
      # Mount persistent data directory
      - moneta_data:/data
      # Mount node_modules to avoid reinstalling on every change
      - frontend_node_modules:/app/frontend/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - monkey_network
    environment:
      - ENV_FILE=/app/env/dev.env
    # Run as root to ensure volume permissions can be set
    user: root

networks:
  monkey_network:
    driver: bridge

volumes:
  pgdata:
  pgadmin_data:
  moneta_data:
  frontend_node_modules:
